// Load argument[0] onto the stack
push argument 0

// Load argument[1] onto the stack
push argument 1

// Perform bitwise XOR (a XOR b = (a OR b) - (a AND b))
or
push argument 0
push argument 1
and
not
and

// Store the result in argument[2]
pop argument 2

// Leave the result at the bottom of the stack
push argument 2
